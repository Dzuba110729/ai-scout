<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Служба launchd для AI-Скаута на всегда включённом Маке.
  Установка — scripts/install_launchd.sh (он подставит реальный путь к проекту
  вместо __PROJECT_DIR__ и положит файл в ~/Library/LaunchAgents).

  Это LaunchAgent (служба пользователя), а не системный демон: нужны Связка ключей
  с входом в подписку Claude Code и Docker Desktop — оба живут в сеансе пользователя.
-->
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>ru.ai-scout.app</string>

    <key>ProgramArguments</key>
    <array>
        <string>/bin/bash</string>
        <string>__PROJECT_DIR__/scripts/run_prod.sh</string>
    </array>

    <key>WorkingDirectory</key>
    <string>__PROJECT_DIR__</string>

    <key>RunAtLoad</key>
    <true/>

    <key>KeepAlive</key>
    <true/>

    <!-- Пауза перед перезапуском после падения, чтобы не молотить в цикле. -->
    <key>ThrottleInterval</key>
    <integer>15</integer>

    <key>StandardOutPath</key>
    <string>__PROJECT_DIR__/logs/app.log</string>
    <key>StandardErrorPath</key>
    <string>__PROJECT_DIR__/logs/app.log</string>

    <key>EnvironmentVariables</key>
    <dict>
        <key>PYTHONUNBUFFERED</key>
        <string>1</string>
    </dict>
</dict>
</plist>
